spring:
  datasource:
    url: jdbc:h2:mem:carddemo_batch_test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  batch:
    jdbc:
      initialize-schema: embedded
  flyway:
    enabled: false
