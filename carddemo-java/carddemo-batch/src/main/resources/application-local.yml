spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/carddemo_batch
    username: ${CARDDEMO_DB_USER:carddemo}
    password: ${CARDDEMO_DB_PASSWORD:carddemo}
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
