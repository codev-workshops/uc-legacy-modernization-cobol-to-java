spring:
  application:
    name: carddemo-gateway
  main:
    web-application-type: reactive
  cloud:
    gateway:
      routes: []

server:
  port: 0

management:
  endpoints:
    web:
      exposure:
        include: health,info
